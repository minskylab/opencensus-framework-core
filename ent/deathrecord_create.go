// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"opencensus/core/ent/deathrecord"
	"opencensus/core/ent/place"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DeathRecordCreate is the builder for creating a DeathRecord entity.
type DeathRecordCreate struct {
	config
	mutation *DeathRecordMutation
	hooks    []Hook
}

// SetReportedDate sets the "reportedDate" field.
func (drc *DeathRecordCreate) SetReportedDate(t time.Time) *DeathRecordCreate {
	drc.mutation.SetReportedDate(t)
	return drc
}

// SetCollectedDate sets the "collectedDate" field.
func (drc *DeathRecordCreate) SetCollectedDate(t time.Time) *DeathRecordCreate {
	drc.mutation.SetCollectedDate(t)
	return drc
}

// SetSinadefRegisters sets the "sinadefRegisters" field.
func (drc *DeathRecordCreate) SetSinadefRegisters(i int) *DeathRecordCreate {
	drc.mutation.SetSinadefRegisters(i)
	return drc
}

// SetMinsaRegisters sets the "minsaRegisters" field.
func (drc *DeathRecordCreate) SetMinsaRegisters(i int) *DeathRecordCreate {
	drc.mutation.SetMinsaRegisters(i)
	return drc
}

// AddPlaceIDs adds the "places" edge to the Place entity by IDs.
func (drc *DeathRecordCreate) AddPlaceIDs(ids ...int) *DeathRecordCreate {
	drc.mutation.AddPlaceIDs(ids...)
	return drc
}

// AddPlaces adds the "places" edges to the Place entity.
func (drc *DeathRecordCreate) AddPlaces(p ...*Place) *DeathRecordCreate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return drc.AddPlaceIDs(ids...)
}

// Mutation returns the DeathRecordMutation object of the builder.
func (drc *DeathRecordCreate) Mutation() *DeathRecordMutation {
	return drc.mutation
}

// Save creates the DeathRecord in the database.
func (drc *DeathRecordCreate) Save(ctx context.Context) (*DeathRecord, error) {
	var (
		err  error
		node *DeathRecord
	)
	if len(drc.hooks) == 0 {
		if err = drc.check(); err != nil {
			return nil, err
		}
		node, err = drc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DeathRecordMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = drc.check(); err != nil {
				return nil, err
			}
			drc.mutation = mutation
			node, err = drc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(drc.hooks) - 1; i >= 0; i-- {
			mut = drc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, drc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (drc *DeathRecordCreate) SaveX(ctx context.Context) *DeathRecord {
	v, err := drc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (drc *DeathRecordCreate) check() error {
	if _, ok := drc.mutation.ReportedDate(); !ok {
		return &ValidationError{Name: "reportedDate", err: errors.New("ent: missing required field \"reportedDate\"")}
	}
	if _, ok := drc.mutation.CollectedDate(); !ok {
		return &ValidationError{Name: "collectedDate", err: errors.New("ent: missing required field \"collectedDate\"")}
	}
	if _, ok := drc.mutation.SinadefRegisters(); !ok {
		return &ValidationError{Name: "sinadefRegisters", err: errors.New("ent: missing required field \"sinadefRegisters\"")}
	}
	if _, ok := drc.mutation.MinsaRegisters(); !ok {
		return &ValidationError{Name: "minsaRegisters", err: errors.New("ent: missing required field \"minsaRegisters\"")}
	}
	return nil
}

func (drc *DeathRecordCreate) sqlSave(ctx context.Context) (*DeathRecord, error) {
	_node, _spec := drc.createSpec()
	if err := sqlgraph.CreateNode(ctx, drc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (drc *DeathRecordCreate) createSpec() (*DeathRecord, *sqlgraph.CreateSpec) {
	var (
		_node = &DeathRecord{config: drc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: deathrecord.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: deathrecord.FieldID,
			},
		}
	)
	if value, ok := drc.mutation.ReportedDate(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: deathrecord.FieldReportedDate,
		})
		_node.ReportedDate = value
	}
	if value, ok := drc.mutation.CollectedDate(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: deathrecord.FieldCollectedDate,
		})
		_node.CollectedDate = value
	}
	if value, ok := drc.mutation.SinadefRegisters(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: deathrecord.FieldSinadefRegisters,
		})
		_node.SinadefRegisters = value
	}
	if value, ok := drc.mutation.MinsaRegisters(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeInt,
			Value:  value,
			Column: deathrecord.FieldMinsaRegisters,
		})
		_node.MinsaRegisters = value
	}
	if nodes := drc.mutation.PlacesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: false,
			Table:   deathrecord.PlacesTable,
			Columns: deathrecord.PlacesPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: place.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// DeathRecordCreateBulk is the builder for creating many DeathRecord entities in bulk.
type DeathRecordCreateBulk struct {
	config
	builders []*DeathRecordCreate
}

// Save creates the DeathRecord entities in the database.
func (drcb *DeathRecordCreateBulk) Save(ctx context.Context) ([]*DeathRecord, error) {
	specs := make([]*sqlgraph.CreateSpec, len(drcb.builders))
	nodes := make([]*DeathRecord, len(drcb.builders))
	mutators := make([]Mutator, len(drcb.builders))
	for i := range drcb.builders {
		func(i int, root context.Context) {
			builder := drcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*DeathRecordMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, drcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, drcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, drcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (drcb *DeathRecordCreateBulk) SaveX(ctx context.Context) []*DeathRecord {
	v, err := drcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
